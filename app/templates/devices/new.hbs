{{page-hamburger-menu title="Add a device" burger=burger user=session.currentUser}}
{{device-form
    deviceModels=model.deviceModels
    onCancel=(route-action "transitionTo" "/")
    onCreate=(route-action "transitionTo" "/")
}}
