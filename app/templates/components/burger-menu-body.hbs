{{#if user}}
  {{#paper-subheader}}Devices{{/paper-subheader}}
  {{#each devices as |device|}}
    {{#menu.item}}
      {{device-paper-item device=device onClick=(route-action "transitionTo" "authenticated.devices.show" device.id)}}
    {{/menu.item}}
  {{/each}}

  {{#menu.item}}
    {{#paper-item class="md-2-line"  onClick=(route-action "transitionTo" "authenticated.devices.new")}}
      {{paper-icon "add" class="md-avatar-icon"}}
      <div class="md-list-item-text">
        <p>Add a device</p>
      </div>
    {{/paper-item}}
  {{/menu.item}}

  {{!-- {{#paper-subheader}}Feedback{{/paper-subheader}} --}}
  {{paper-divider}}

  {{#feedback-menu-item
    menu=menu
    showDialog=false
    title="Share a review on Amazon?"
    body="Your thoughts mean everything to us!"
    confirmLabel="Post A Review"
    onConfirm=(route-action "hopTo" hrefForAmazon)}}
      {{paper-icon "sentiment-very-satisfied" class="md-avatar-icon"}}
      <div class="md-list-item-text">
        <p>Happy?</p>
      </div>
  {{/feedback-menu-item}}

  {{#feedback-menu-item
    menu=menu
    showDialog=false
    title="💁 We're here to help!"
    body="Visit the website to see instructions, tutorials, our FAQ and excellent customer service."
    confirmLabel="Goto Website"
    onConfirm=(route-action "hopTo" hrefForAmazon)
    cancelLabel="Nevermind"
  }}
    {{paper-icon "sentiment-neutral" class="md-avatar-icon"}}
      <div class="md-list-item-text">
        <p>Disappointed?</p>
      </div>
  {{/feedback-menu-item}}
{{/if}}
